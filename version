<1.5.2>
- Fixed: loan applications now upgrade older loan schemas with every required column
- Fixed: bank and character interest-rate lookup safely falls back to the default rate
- Fixed: accountless loan applications now create the dedicated loan account promised by the UI
- Fixed: invalid loan durations and out-of-range interest rates are rejected
